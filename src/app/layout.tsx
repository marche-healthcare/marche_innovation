import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Marche Innovation - Custom Medical & Engineering Equipment",
  description: "Technology-driven innovation company that designs, develops, and manufactures custom medical and engineering equipment for hospitals, clinics, research institutions, and industrial clients.",
  keywords: ["medical equipment", "engineering equipment", "custom manufacturing", "innovation", "healthcare technology", "biomedical engineering"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
